import { FilterItemsByVendorPipe } from './filter-items-by-vendor.pipe';

describe('FilterItemsByVendorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemsByVendorPipe();
    expect(pipe).toBeTruthy();
  });
});
